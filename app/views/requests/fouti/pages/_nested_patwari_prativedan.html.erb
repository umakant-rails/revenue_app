<!-- halka patwari prativedan -->
<div class="page-a4 only-print" data-request-target="page" data-name="patwari-prativedan" style="display: none;">
  <div class="col-md-12">
    <div class="row pd-top-2 padding-5">
      <div class="fnt-24 fnt-b fnt-u">
        <% village = Village.find(village_id) %>
        <center>
          कार्यालय पटवारी हल्का नंबर _ _ <%= village.halka_number %>, <%= village.halka_name %>
        </center>
      </div>
    </div>
    <div class="row mrgn-top-20 padding-5">
      <div class="paragraph-35">
        <div class="fnt-20">प्रति,<br/></div>
        <div class="padding-6">
          श्रीमान तहसीलदार/नायब तहसीलदार महोदय<br/>
          वृत्त <strong><%= village.ri %></strong>, तहसील <strong><%= village.tehsil %></strong>
        </div>
        <div class="">
          <strong>विषय &nbsp;:- </strong> फौती/नामांतरण के सम्बन्ध में प्रतिवेदन |<br/>
        </div>
        <div class="">
          महोदय  जी,<br/>
        </div>
        <div class="txt-indent-3">
          <% dead_persons = request.participants.fout_person %>

          उक्त विषयान्तर्गत लेख है कि, मौजा <strong><%= request.village.halka_name %></strong> प.ह.न. <strong><%= request.village.halka_number %></strong> तहसील <strong><%= request.village.tehsil %></strong> जिला <strong><%= request.village.district %></strong> के आवेदक <%= raw applicant_name(request, true) %> द्वारा अपने माता/पिता/अन्य

          <% if request.participants.fout_person.length > 1 %>
            <%=raw dead_persons.collect{ |p| [participant_name(p)] }.join(", ") %> की मृत्यु दिनांक क्रमशः <strong><%= request.participants.fout_person.collect{ |p| [format_dt(p.death_date)] }.join(", ") %></strong>
          <% else %>
            <% f_person = request.participants.fout_person.first %>
            <strong><%= fout_persons(request) %></strong> की मृत्यु दिनांक <strong><%= request.participants.fout_person.collect{ |p| [format_dt(p.death_date)] }.join(", ") %></strong>
          <% end %> 

           को हो जाने के कारण नामांतरण/फौती हेतु आवेदन पत्र श्रीमान जी के समक्ष प्रतुत किया है | 
        </div>
      
        <% khasras = request.khasras.where(village_id: village.id) %>
        <div class="txt-indent-3">
          यह कि मौजा <strong><%= village.halka_name %></strong> प.ह.न. <strong><%= village.halka_number %></strong> तहसील <strong><%= village.tehsil %></strong> जिला <strong><%= village.district %></strong> की भूमि खसरा नंबर <strong><%= khasras.pluck(:khasra).join(", ") %></strong> रकबा <strong><%= khasras.collect{ |k| k.rakba + " हे."}.join(", ") %></strong> कुल रकबा <strong><%= khasras.collect{ |k| k.rakba.to_f}.sum %> हे.</strong> पर वर्तमान अभिलेख में 
            <%=raw dead_persons.collect{ |p| [participant_name(p)] }.join(", ") %> का/के नाम दर्ज है| 
        </div>
        <div class="txt-indent-3">
          चूकिं मृतक ग्राम <strong><%= village.village %></strong> का निवासी नहीं है मृतक <strong><%= request.village.village %></strong> ग्राम का निवासी है अतः मृतक के वारसान की जानकारी पटवारी हल्का <strong><%= request.village.halka_name %></strong> से लिया जाना उचित होगा | 
        </div>
  
        <div class="txt-indent-4">
          अतः प्रतिवेदन सादर सम्प्रेषित है |
        </div>
        <br/><br/>
        <div>
          <div class="fRight">
            <center>- - - - - - - - - - - - - - - - -</center>
            <center><strong>हस्ताक्षर</strong><center>
            <center><strong>पटवारी हल्का <%= village.halka_name %></strong><center>
          </div>
        </div> 
      </div>
    </div>
  </div>
</div>