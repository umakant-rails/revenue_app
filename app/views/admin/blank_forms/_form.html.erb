<%= form_with(model: admin_blank_form) do |form| %>
  <% if admin_blank_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_blank_form.errors.count, "error") %> prohibited this admin_blank_form from being saved:</h2>

      <ul>
        <% admin_blank_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
